/*let firstName = 'John';
let age = 16;

age >= 21 ? console.log(firstName + ' can drink beer!') :
console.log(firstName + ' cannot drink beer!');

//Assigning a variable using a ternary operator
let drink = age >= 21 ? 'beer' : 'no beer';

//Assigning a variable using an if else
if (age < 21) {
    let drink = 'beer';
}else {
    let drink = 'no beer';
}

//Using the Switch Statement
let job = 'driver';

switch(job){
    case 'teacher':
        console.log(firstName + ' teaches kids to code,');
        break;
    case 'driver':
        console.log(firstName + ' drives for Uber.');
        break;
    case 'designer':
        console.log(firstName + ' designs websites.');
        break;
    default:
        console.log(firstName + ' is unemployed.');
}

let age = 21;

switch (true) {
    case age < 13:
        console.log(firstName + ' is under 13');
        break;
    case age >= 13 && age < 20:
        console.log(firstName + ' is a teenager.');
        break;
    case age >= 20 && age < 30:
        console.log(firstName + ' is an adult.')
        break;
    default:
        console.log(firstName + ' is an older person.');
}*/




